export default class Minimax {
  //use static functions to avoid having to instanciate. moved all minimax functions here just for cleaner code.

  static findBestMove(board) {
    return 1;
  }

  static minimax(board, depth, isMaximizingPlayer) {}

  static isMovesLeft(board) {}
}
